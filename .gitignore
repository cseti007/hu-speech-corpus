# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtualenvs
.venv/
venv/
env/

# Third-party repos checked out for local use (e.g. facebookresearch/voxpopuli)
external/

# Internal experiments / POC scripts — not part of the public release
bin/poc/

# Editor
.vscode/
.idea/
*.swp
*~

# Claude Code session files (per-machine config / cache)
.claude/

# Private working docs (JOURNEY, snapshots, schema drafts, etc.)
notes/

# Secrets — never commit
.hf_token
*.token

# Data lives elsewhere on the SSD; never accidentally commit if someone copies it in
raw/
processed/
cache/
*.wav
*.mp3
*.ogg
*.flac
*.opus
*.m4a
*.parquet
*.tar
*.tar.gz
*.jsonl

# OS
.DS_Store
Thumbs.db
